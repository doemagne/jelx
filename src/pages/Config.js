const Config = () => {
    return (
        <div>Configuration</div>
    )
}

export default Config;
/*
let nv = document.body.innerHTML = `<video id=\"nv\" autoplay></video>`;
let ctsr = {video:true,audio:true};
navigator.mediaDevices.getUserMedia(ctsr).then(function(stream) {
    window.stream = stream;
    let v = document.getElementById("nv");
    v.srcObject = stream;
});
*/